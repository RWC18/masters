apiVersion: v1
kind: Service
metadata:
  name: vai-server
spec:
  type: ClusterIP
  selector:
    app: vai-server
  ports:
    - protocol: TCP
      port: 443 
      targetPort: 8010 
      name: http
