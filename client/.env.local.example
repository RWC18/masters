# Private — used only by Next.js rewrites proxy. Never sent to browser.
BACKEND_URL=http://localhost:7777

# Public — exposed to the browser bundle (for direct AI service calls)
NEXT_PUBLIC_API_AUTH=
NEXT_PUBLIC_AI_URL=
NEXT_PUBLIC_E2E_AI_URL=
NEXT_PUBLIC_X_APP_AUTH=
