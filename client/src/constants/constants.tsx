/** Set to true when AI Avatar generation is ready for users. */
export const AVATAR_GENERATION_ENABLED = false;

export const LOCALSTORAGE_KEYS = {
  ACCESS_TOKEN: 'access_token_vai',
  THEME_MODE: 'theme_mode_vai',
};

export const LOGIN_POPUP_TABS = {
    LOGIN: 'login',
    REGISTER: 'register',
}