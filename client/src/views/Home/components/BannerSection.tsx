import { Box } from '@mui/material';
import React from 'react';
import { HomeStyles } from '../Home.styles';

const BannerSection = () => {
  return <Box sx={HomeStyles.banner} />;
};

export default BannerSection;

